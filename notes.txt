
# Create a virtual envirnoment
python3 -m venv .venv
source .venv/bin/activate

# deactivate
deactivate

brew install ruby@2.7
brew install rdkit
gem install jekyll bundler

# gem install classifier-reborn
# gem install jekyll-archives
# gem install jekyll-diagrams
# gem install jekyll-email-protect
# gem install jekyll-feed
# gem install jekyll-imagemagick
# gem install jekyll-link-attributes
# jekyll-minifier
# jekyll-paginate-v2
# jekyll-scholar
# jekyll-sitemap
# jekyll-twitter-plugin
# jemoji
# mini_racer
# unicode_utils
# feedjira
# httparty

ruby -v
gem -v
jekyll -v

============== Fix Authentication of local repo VS Code with Github ==============
# Instructions by: https://www.golinuxcloud.com/set-up-visual-studio-code-remote-ssh-github/#SSH
git config --global user.name isymeonidis
git config --global user.email iraklis.symeonidis@icloud.com
ssh-keygen -t ed25519 -C "iraklis.symeonidis@icloud.com"
eval "$(ssh-agent -s)"

# https://docs.github.com/en/authentication/connecting-to-github-with-ssh/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent
ls -al ~/.ssh
cat ~/.ssh/id_ed25519.pub
open ~/.ssh/config
# Host github.com
#   AddKeysToAgent yes
#   UseKeychain yes
#   IdentityFile ~/.ssh/id_ed25519

ssh-add ~/.ssh/id_ed25519

ssh-add ~/.ssh/id_ed25519 # ssh-add --apple-use-keychain ~/.ssh/id_ed25519 (if passphrase exist)
pbcopy < ~/.ssh/id_ed25519.pub
# https://docs.github.com/en/authentication/connecting-to-github-with-ssh/adding-a-new-ssh-key-to-your-github-account (guide)

ssh -T git@github.com # testing

====================================================================================


============== UPDATE ==============
git clone https://github.com/isymeonidis/isymeonidis.github.io.git


bundle install (on the master branch)
 
bundle exec jekyll serve (on the master branch)
# ps aux |grep jekyll |awk '{print $2}' | xargs kill -9

bin/deploy --user (on the master branch)


====================================================================================



# Reset git
git fetch origin
git reset --hard origin/master

# Other unused commands
# <!-- https://jekyllrb.com/docs/installation/macos/ -->
# brew install chruby ruby-install xz
# ruby-install ruby 3.1.3

# gem install jekyll bundler
# ruby -v
# gem -v
# jekyll -v

#  bundle install

# git remote add origin git@github.com:isymeonidis/isymeonidis.github.io.git
# git pull origin master